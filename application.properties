# Server Configuration
server.port=8080
server.address=0.0.0.0
server.servlet.context-path=/

# Application
spring.application.name=file-damage-analyzer

# Logging
logging.file.name=/var/log/file-damage-analyzer/application.log
logging.level.org.example=INFO
logging.level.org.springframework=INFO
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
logging.pattern.console=%d{HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# File Analysis Settings
file-analysis.max-differences-per-file=100
file-analysis.buffer-size=8192

# CORS
# spring.web.cors.allowed-origins=http://localhost:8080
# spring.web.cors.allowed-methods=GET,POST,PUT,DELETE